'use client';

import { useState } from 'react';
import Link from 'next/link';
import { Header } from '@/components/Header';
import { popiaQuestions } from '@/lib/popia-questions';

export default function PopiaQuizPage(){const[answers,setAnswers]=useState<number[]>(new Array(popiaQuestions.length).fill(-1));const[result,setResult]=useState<{percentage:number;passed:boolean}|null>(null);const[saving,setSaving]=useState(false);const[saveMessage,setSaveMessage]=useState('');const allAnswered=answers.every(answer=>answer!==-1);function selectAnswer(questionIndex:number,optionIndex:number){const updated=[...answers];updated[questionIndex]=optionIndex;setAnswers(updated);setResult(null);setSaveMessage('')}async function submitAssessment(){if(saving||!allAnswered)return;setSaving(true);setSaveMessage('');try{const response=await fetch('/api/training/popia-assessment',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({answers})});const data=await response.json();if(!response.ok){setSaveMessage(data.error||'The assessment could not be recorded.');return}setResult({percentage:data.percentage,passed:data.passed});setSaveMessage(data.passed?'✓ Assessment result verified and saved. POPIA training completed.':'Assessment result saved. You must achieve 80% to pass.')}catch{setSaveMessage('The assessment could not be recorded. Please try again.')}finally{setSaving(false)}}return <main className="shell"><Header/><section className="hero"><span className="pill">Assessment</span><h1>POPIA & Data Protection Assessment</h1><p className="muted">Answer all 10 questions. A minimum score of 80% is required to pass.</p></section><section className="section">{popiaQuestions.map((question,questionIndex)=><div className="card" key={question.q} style={{marginBottom:18}}><h2 style={{fontSize:20}}>{questionIndex+1}. {question.q}</h2><div style={{display:'grid',gap:10}}>{question.options.map((option,optionIndex)=><label key={option} style={{display:'flex',alignItems:'center',gap:10,cursor:'pointer'}}><input type="radio" name={`question-${questionIndex}`} checked={answers[questionIndex]===optionIndex} onChange={()=>selectAnswer(questionIndex,optionIndex)}/><span>{option}</span></label>)}</div></div>)}</section><section className="section"><div className="card"><button className="button" disabled={!allAnswered||saving} onClick={submitAssessment} style={{border:0,cursor:allAnswered?'pointer':'not-allowed',opacity:allAnswered?1:.5}}>{saving?'Saving assessment...':'Submit assessment'}</button>{saveMessage&&<p role="status" style={{marginTop:16,fontWeight:600}}>{saveMessage}</p>}{result&&<div style={{marginTop:24}}><h2>Score: {result.percentage}%</h2>{result.passed?<><p className="ok">✓ Assessment passed</p><p>You have successfully completed the POPIA & Data Protection assessment.</p></>:<><p className="warn">Assessment not passed</p><p>You need at least 80%. Please review the POPIA training and try again.</p><Link href="/training/popia-data-protection" className="button">Review POPIA training</Link></>}</div>}</div></section><section className="section"><Link href="/">← Back to My Portal</Link></section></main>}
