'use client';

import { FormEvent, useState } from 'react';
import Link from 'next/link';
import { Header } from '@/components/Header';

export default function PrivacyPage(){const[saving,setSaving]=useState(false);const[message,setMessage]=useState('');async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setSaving(true);setMessage('');const form=event.currentTarget;const fd=new FormData(form);try{const response=await fetch('/api/privacy-requests',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({type:fd.get('type'),details:fd.get('details')})});const data=await response.json();setMessage(data.message||data.error||'Unable to record your request.');if(response.ok)form.reset()}catch{setMessage('Unable to record your request. Please contact HR.')}finally{setSaving(false)}}return <main className="shell"><Header/><section className="hero"><span className="pill">POPIA</span><h1>Employee privacy notice</h1><p className="muted">How Isitha Global uses and protects information in this staff portal.</p></section><section className="section"><div className="card"><h2>Why we hold your information</h2><p>Isitha Global uses your identity, contact, payroll, banking, tax, emergency-contact, training, contract and leave information to manage the employment relationship, meet legal duties, administer payroll and benefits, protect health and safety, and keep required HR records.</p><h2>Sensitive information</h2><p>ID documents, banking details and medical certificates receive restricted access. Medical certificates are used only for authorised leave administration.</p><h2>Where information is stored</h2><p>The portal database is hosted in Sweden. Isitha Global must maintain the lawful cross-border transfer basis and appropriate safeguards for information leaving South Africa.</p><h2>Retention and sharing</h2><p>Information is limited to authorised Isitha Global staff and approved service providers who need it for these purposes. Records are retained only for the employment, legal and operational periods approved by Isitha Global, then securely deleted.</p><h2>Your rights</h2><p>You may request access, correction or deletion, or object to particular processing. Some employment or legal records may need to be retained even after a deletion request; HR will explain the outcome.</p></div></section><section className="section"><div className="card"><h2>Submit a privacy request</h2><form className="admin-form" onSubmit={submit}><label htmlFor="privacy-type">Request type<select id="privacy-type" name="type" required defaultValue=""><option value="" disabled>Select</option><option value="access">Access my information</option><option value="correction">Correct my information</option><option value="deletion">Delete information</option><option value="objection">Object to processing</option></select></label><label htmlFor="privacy-details">Details<textarea id="privacy-details" name="details" rows={5} required/></label><div className="admin-form-actions"><button className="button" disabled={saving}>{saving?'Submitting...':'Submit to HR'}</button></div></form>{message&&<p role="status"><strong>{message}</strong></p>}</div></section><section className="section"><Link href="/">← Back to My Portal</Link></section></main>}
